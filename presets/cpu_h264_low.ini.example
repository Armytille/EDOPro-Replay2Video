# CPU H.264 - Low quality (fast render, small file)
# Compatible with every player and platform.
# Rename to config.ini and place next to ygopro.exe.

workdir=D:\ProjectIgnis
resolution=1280x720
fps=30
codec=libx264
preset=veryfast
crf=28
scale_filter=bilinear
margin=0.10
speed=1.0
